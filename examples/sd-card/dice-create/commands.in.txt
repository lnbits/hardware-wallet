# Create and unlock a persistent 24-word wallet from 100 keypad-entered d6 rolls.
# The new wallet password must be at least 8 characters.
/create replace-with-a-strong-password

# Example follow-up command; the newly created wallet is already authenticated.
/xpub Testnet m/84'/1'/0'

# Just "xpub" will fetch xpub for Mainnet m/84'/0'/0'
/xpub
